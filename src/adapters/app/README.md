# Adaptador App (Integración con tu MVP)
Contrato sugerido:
- Entrada: { tipo_evento, payload }
- Salida: { accion, mensaje|archivo }
Cuando toque integrar, agregamos un `api.ts` con endpoints y auth por token.
